/**
 * @file device.h
 * @brief Device abstraction layer — defines a common interface for compute devices
 *        (CPU/GPU).
 *
 * This module provides device-agnostic tensor operation support, allowing upper-level
 * code to perform tensor operations without concern for the specific compute backend.
 */
